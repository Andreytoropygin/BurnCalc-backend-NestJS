<!DOCTYPE html>
<html lang="ru">
{{> head}}
<body>
    {{> header}}

    <main class="main-content">
        <div class="container">
            {{#if request}}
                <p class="page-title">Заявка №{{request.id}}</p>

                <!-- Блок исходных данных -->
                <section class="data-section">
                    <p class="section-title">Исходные данные</p>
                    <div class="input-grid">
                        <div class="input-group h2o_volume">
                            <label for="water-volume">Объем выделившегося водяного пара, л:</label>
                            <input type="number" name="h2o_volume" id="water-volume" value="{{request.h2o_volume}}" placeholder=1>
                        </div>
                        <div class="input-group">
                            <label for="co2-volume">Объем выделившегося углекислого газа, л:</label>
                            <input type="number" name="co2_volume" id="co2-volume" value="{{request.co2_volume}}" placeholder=1>
                        </div>
                        <div class="input-group">
                            <label for="sample-description">Описание образца:</label>
                            <input type="text" name="sample_description" id="sample-description" value="{{request.sample_description}}" placeholder="Описание...">
                        </div>
                    </div>
                </section>

                <!-- Блок состава заявки -->
                <section class="composition-section">
                    <h2 class="section-title">Состав заявки</h2>
                    {{#if combustions}}
                        {{#each combustions}}
                            <!-- Карточка соединения -->
                            <div class="request-combustion-card">
                                <div class="card-header">
                                    <h3 class="request-combustion-name">{{this.title}}</h3>
                                </div>
                                <div class="card-body">
                                    <div class="request-combustion-image">
                                        <img src="/images/{{this.image_file_name}}" alt="Модель молекулы">
                                    </div>
                                    <div class="request-combustion-properties">
                                        <div class="property-group">
                                            <label for="specific-h2o-volume-{{this.id}}">Эталонный объем водяного пара сгорания, л/моль</label>
                                            <p>{{this.specific_h2o_volume}}</p>
                                        </div>
                                        <div class="property-group">
                                            <label for="specific-co2-volume-{{this.id}}">Эталонный объем углекислого газа сгорания, л/моль</label>
                                            <p>{{this.specific_co2_volume}}</p>
                                        </div>
                                        <div class="property-group">
                                            <label for="request-comment-{{this.id}}">Комментарий</label>
                                            <input type="text" id="request-comment-{{this.id}}" value="{{this.request_comment}}">
                                        </div>
                                        <div class="property-group property-result">
                                            <label for="amount-{{this.id}}">Количество в образце, моль</label>
                                            <p>{{this.amount}}</p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        {{/each}}
                    {{else}}
                        <p>Соединения не добавлены</p>
                    {{/if}}
                </section>
            {{else}}
                <h2>{{title}}</h2>
            {{/if}}
        </div>
    </main>

</body>
</html>