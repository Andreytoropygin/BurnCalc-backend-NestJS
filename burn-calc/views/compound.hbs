<!DOCTYPE html>
<html lang="ru">
{{> head}}
<body>
    {{> header}}
    <main class="main-content">
        <div class="container compound-page-container">
            
            {{#if compound}}
                <!-- Левая колонка: Видео -->
                <div class="media-column">
                    <div class="media-wrapper">
                        <video autoplay loop muted controls width="100%" class="media-placeholder">
                            <source src="/videos/{{compound.videoFileName}}" type="video/mp4">
                        </video>
                    </div>
                </div>

                <!-- Правая колонка: Информация о соединении -->
                <div class="info-column">
                    <div class="compound-info-card">
                        <p class="compound-info-card-title">{{compound.title}}</p>
                        <div class="compound-info-card-content">
                            <div class="tabs">
                                <button class="tab-btn active">Описание</button>
                                <button class="tab-btn">Комментарии</button>
                            </div>

                            <div class="tab-content">
                                <table class="properties-table">
                                    <tbody>
                                        <tr>
                                            <td>Класс</td>
                                            <td>{{compound.class}}</td>
                                        </tr>
                                        <tr>
                                            <td>Молярная масса</td>
                                            <td>{{compound.molar_mass}} г/моль</td>
                                        </tr>
                                        <tr>
                                            <td>Брутто-формула</td>
                                            <td>{{formula}}</td>
                                        </tr>
                                    </tbody>
                                </table>

                                <div class="molecule-image">
                                    <img src="/images/{{compound.imageFileName}}" alt="Модель молекулы">
                                </div>

                                <p class="description-text">{{compound.description}}</p>
                            </div>
                            <form action="/burn-calc/add-to-cart" method="POST">
                                <input type="hidden" name="compoundId" value="{{compound.id}}">
                                <input type="hidden" name="redirectTo" value="/burn-calc/compound/{{compound.id}}">
                                <button type="submit" class="add-to-request-btn">Добавить в заявку</button>
                            </form>
                        </div>
                    </div>
                </div>
            {{else}}
                <h2>{{title}}</h2>
            {{/if}}

        </div>
    </main>

</body>
</html>